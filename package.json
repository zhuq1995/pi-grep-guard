{
  "name": "pi-grep-guard",
  "version": "1.0.0",
  "description": "pi 扩展：自动为 bash 里的 grep 追加 --exclude-dir，跳过 .svn/.vs/node_modules 等重量级目录",
  "keywords": ["pi-package"],
  "license": "MIT",
  "type": "module",
  "files": ["src"],
  "pi": {
    "extensions": ["./src/index.ts"]
  },
  "scripts": {
    "test": "node --experimental-strip-types test/rewrite.test.mjs"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*"
  }
}
